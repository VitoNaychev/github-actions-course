name: Repository Events
on:
  push:
  pull_request:
  issues:
      types: [opened, edited]
    


jobs:
  checkout-test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
      - uses: actions/checkout@v3
      